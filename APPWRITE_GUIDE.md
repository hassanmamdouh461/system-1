# دليل إعداد Appwrite 🚀

لربط النظام مع Appwrite، اتبع الخطوات التالية:

## 1. الحصول على البيانات من Appwrite

1. سجل الدخول إلى [Appwrite Cloud](https://cloud.appwrite.io/).
2. أنشئ مشروع جديد (Create Project).
3. انسخ **Project ID** من الإعدادات.
4. اذهب إلى **API Keys** وأنشئ مفتاح جديد، ثم انسخ الـ **API Key Secret**.

## 2. تحديث ملف .env

افتح ملف `backend/.env` والصق البيانات:

```env
APPWRITE_PROJECT_ID=your_project_id_here
APPWRITE_API_KEY=your_api_key_here
```

## 3. تشغيل سكربت الإعداد التلقائي

قمنا بإنشاء سكربت يقوم بعمل كل شيء نيابة عنك!

```bash
cd backend
node setup_appwrite.js
```

سيقوم السكربت بـ:

- إنشاء قاعدة البيانات.
- إنشاء الجداول (Products, Customers, Invoices, Sales).
- طباعة الـ IDs الجديدة.

## 4. الخطوة الأخيرة

انسخ الـ IDs التي ظهرت في الشاشة وضعها في ملف `.env`.

---

الآن أصبح النظام مرتبطاً بـ Appwrite! 🎉
